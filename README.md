# mearm-controller

Browser based MeArm or any other robot arm that can be controlled over serial. 
Takes a four integer comma separated packet eg (100,30,60,140) where these are the values the servo motors are to be set to in degrees.
